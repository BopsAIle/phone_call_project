Planning documents live here, one file per feature: `YYYY-MM-DD-<feature-slug>.md`.

A feature large enough to be built in stages may add a `phases/` subfolder holding one detailed
execution plan per phase, indexed by its own README. The feature-level plan stays the entry point and
links out to them — see [phases/](phases/README.md) for the current example.
